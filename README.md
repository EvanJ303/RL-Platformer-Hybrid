# RL-Platformer
Deep Q-Learning applied to a simple platformer game. The agent utilizes a deep Q-network to efficiently jump between platforms and touch objectives.

## Dependencies
PyTorch, Pygame, Numpy

## Usage 
Create a folder called "data" in your working directory and create a folder called "models" inside of it. Run training.py to train and save the agent and run inference.py to showcase it.